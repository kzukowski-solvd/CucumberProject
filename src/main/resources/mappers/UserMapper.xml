<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.solvd.carina.demo.gui.interfaces.UserMapper">

    <resultMap id="userResultMap" type="com.solvd.carina.demo.gui.models.User">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="password" column="password" />
    </resultMap>

    <select id="getUserById" resultMap="userResultMap">
        SELECT * FROM User WHERE id = #{id}
    </select>

    <insert id="insertUser" parameterType="com.solvd.carina.demo.gui.models.User">
        INSERT INTO User (name, password) VALUES (#{name}, #{password})
    </insert>

</mapper>
