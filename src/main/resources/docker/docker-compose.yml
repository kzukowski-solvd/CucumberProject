services:
  db:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_DATABASE: 'cucumber'
      MYSQL_ROOT_PASSWORD: 'pass'
    ports:
      - '3306:3306'
    expose:
      - '3306'
    volumes:
      - ./db-dump:/docker-entrypoint-initdb.d/